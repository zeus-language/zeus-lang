fn main() : i32 {
    let a : string = string::from_slice("á");
    let slice = a.as_slice();
    println(a);
    println(a.length() as i32); // should output 1
    println(slice.length); // should output 2
    return 0;
}