use std::math;

fn main() {
    let x = 0.5;
    let y = math::sin(x);
    let z = math::cos(x);
    let r = math::sqrt(x * x + y * y + z * z);

    println("sin($x) = $y");
    println("cos($x) = $z");
    println("sqrt($x^2 + $y^2 + $z^2) = $r");
}