use std::math;

fn main():i32 {
    let x: float = 0.5;
    let y: float = sin(x);
    let z: float = cos(x);
    let r: float = sqrt(x * x + y * y + z * z);

    println("sin($x) = $y");
    println("cos($x) = $z");
    println("sqrt($x^2 + $y^2 + $z^2) = $r");
    return 0;
}